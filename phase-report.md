# phase-report · P0：冻结事实 + 修三个阻塞

日期：2026-09-11 · 依据：执行规格 §11 P0 / §4 B01–B03 / §12 验收矩阵
状态：**P0 完成，三项阻塞实测通过；P1–P4 未开始**

---

## 1. 这一阶段做了什么

| # | 交付 | 位置 |
|---|---|---|
| 1 | 现场事实冻结（实例/端口/vault/资源版本/WIP/红线） | `LOCAL-BASELINE.md` |
| 2 | WIP 备份（未丢任何暂存/未暂存/未跟踪改动） | `../backups/marginalia-wip-20260911-105339.tgz` + git 对象 `38f24cc` |
| 3 | 真机测试台（Chrome + CDP，全部走真实鼠标/滚轮事件） | `tools/p0-harness.mjs` |
| 4 | B01 修复：原文可见批注 | `public/app.js`（锚点匹配层）、`server.mjs`（字段白名单） |
| 5 | B02 修复：右栏点击职责 | `public/app.js`（卡片事件） |
| 6 | B03 修复：触控板缩放 | `public/app.js`（wheel / zoomAt / schedulePdfZoom） |
| 7 | 可复跑的验收脚本 | `tools/p0-b01.mjs`、`p0-b01-zoom.mjs`、`p0-b02.mjs`、`p0-b03.mjs`、`p0-b01-diag.mjs` |

**没有做**：P1 界面统一、P2 持续要求与 Agent 闭环、P3 安装壳、P4 官网。也没有改 PPTX/DOCX 能力。

---

## 2. B01 原文看不到批注

### 根因（实测，非推测）

用真实鼠标在中文 PDF 上跨行拖选后写意见，落盘与 API 回读都正常，**重开后一个标记都不画**。逐字节比对 sidecar 里的 quote 与重开后重建的文本索引：

| | 内容 |
|---|---|
| 保存的 `quote`（来自 `Selection.toString()`） | `…技术附录 A` `\n` `Working draft…` |
| 重开后 `buildIndex` 拼出的文本 | `…技术附录 AWorking draft…`（**无分隔符**） |
| `indexOf(quote)` | **-1** → `locate()` 返回 null → 0 个 mark |
| 两侧都去掉空白后再 `indexOf` | **0（命中）** |

英文样本侥幸通过，是因为那一页的文字层恰好也带 `\n`；中文样本必挂。这解释了"时好时坏"。

### 修法（按规格要求把「原文」与「匹配用文本」分开）

- `buildIndex` 现在同时产出：`text`（原文，只用于取字形偏移）、`norm`（匹配用规范文本：去空白 + NFKC 全角/连字归一）、`raw`（raw 下标 → DOM 位置）、`normToRaw`（norm 下标 → raw 下标）。
- `locate` 在 `norm` 空间搜索，再用 `normToRaw` 映射回真实 DOM 位置，`wrapRange` 拿到的仍是精确字形偏移。
- 新增**页级精确恢复**：建批注时存 `pageIndex` + `textOffset` + `textLen` + `pageFp`（该页文本指纹）。指纹一致 → 直接按偏移落点（零猜测）；内容变了 → 指纹对不上，自动退回规范化搜索。**绝不盲用旧坐标。**
- PDF 改成**按页建索引**：跨页搜索既慢又容易钉到别页的同名句。
- `server.mjs` 放行上述字段；PATCH 时若 `quote` 变了，旧的精确偏移立即作废。

### 证据

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 中文多页 PDF（跨行拖选 + 中文意见 + 重开） | 10/13（标记数 **0**） | **13/13**，连跑 4 次全绿 |
| 英文单页 PDF（回归） | 13/13 | **13/13**，连跑 3 次全绿 |
| 50 / 100 / 153 / 202% 四档缩放 | — | **10/10**，标记数量不重影、分片贴合文字 |

截图：`/tmp/coeditor-p0/evidence/b01-reopen.png`、`b01-zoom-{50,101,153,202}.png`
断言明细：`/tmp/coeditor-p0/evidence/b01.json`、`b01-zoom.json`、`b01-diag-研究设计-技术附录.pdf.json`

### 已知局限

- PDF 文字层把空格、标点切成独立 span，一条跨行批注会形成多个 `.anchor` 分片（实测 14 片）。虚线因此是连续的，但"批注数 ≠ 标记数"。
- 标点分片（如 `·`）的字形框远宽于墨迹，指针命中测试会落到邻居上——纯视觉无影响（见截图），已按此口径取样。
- **A03/A04 未覆盖**：原文整段替换、重复句、跨页、旋转页仍未测。需要更多样本。

---

## 3. B02 右栏点击

### 复现（修复前实测 6/9）

| 失败项 | 实测 |
|---|---|
| 拖选卡片文字不改变选中项 | ❌ 选中项被改到另一张卡 |
| 拖选卡片文字不滚动正文 | ❌ `scrollTop` 100 → 0（**正文跳回顶部**） |
| 点引用不改变选中项 | ❌ 点了引用，选中项变了，且触发正文跳转 |
| 重复点击 20 次 | 6/20 次选中的不是被点的卡 |

### 根因

1. `selectCard` 挂在 **mousedown**，而它在切换分组时会**重建整个卡片列表**——pointerdown 到 click 之间被点的那张卡已被换掉，点击结果变成随机。
2. 卡片上的 `click` 处理器对拖选文字后浏览器补发的 click 也会执行 **flash + centerOn**，把正文滚走。
3. `.c-quote` 的展开处理器与卡片 click 处理器**同时**响应，点引用既展开又跳转（规格 §3.6 要求"只展开引用"）。

### 修法

- 选中与定位统一移到 **click 阶段**，并顺序排除三种意图：按钮/输入/折叠/**引用**、指针位移 > 4px、卡内存在文字选区。
- `selectCard(id, { rebuildIfHidden })`：**用户点右栏里的卡时不重建列表**（卡已经在眼前）；只有从正文侧选中不在当前分组的批注才切分组重画。

### 证据

修复后 **9/9，连跑 3 次全绿**（新建 3 张卡 + 20 次点击 + 拖选 + 编辑 + 引用）。
明细：`/tmp/coeditor-p0/evidence/b02.json`，截图 `b02-rail.png`。

---

## 4. B03 触控板缩放

### 根因

- `deltaY < 0 ? 1.1 : 1 / 1.1` —— 每个 wheel 事件被当成一个固定倍率按钮，**忽略手势幅度与 `deltaMode`**。触控板一次轻捏连发几十个事件，轻微动作也跳档。
- `zoomAt` 在 PDF 异步重渲染**之前**按 ratio 改 `scrollTop`，会被**旧尺寸夹紧**；渲染完成也无人恢复 → 手势中心漂移。

### 修法

- `deltaMode` 归一化（0 像素 / 1 行 / 2 页）→ `requestAnimationFrame` 帧内合并 → **连续指数映射** `exp(-dy × 0.0022)`，单帧倍率变化封顶 ±18%。
- PDF 缩放改为**锚点式恢复**：记下光标下的「页码 + 页内归一化点 + 屏幕偏移」，新尺寸渲染完成后把同一点恢复到同一屏幕位置；引入渲染代次 `pdfZoomGen`，**旧任务不得覆盖新尺寸或新锚点**。
- 普通双指滚动不再被拦截，交还原生滚动；画布态才平移。

### 证据

**9/9，连跑 2 次全绿**：

| 断言 | 实测 |
|---|---|
| 缓慢捏合不跳档 | 单事件倍率 1.06（旧实现固定 1.10 且与幅度无关） |
| 倍率随手势幅度变化 | 小幅度 1.061 / 大幅度 1.178，对数比 2.77 |
| 手势中心锚点漂移 ≤ 8px | **0.1 px** |
| 上下限夹紧 | 300% / 50% 精确 |
| 普通滚动不改倍率、不被拦截 | ✅ |
| 侧栏滚轮不影响文档 | ✅ |

明细：`/tmp/coeditor-p0/evidence/b03.json`。

> **真机触控板验收仍未做**（规格 §B03 要求）。CDP 合成只作自动回归，不能替代人手。

---

## 5. 过程中发现、但本阶段未修

| 发现 | 影响 | 建议 |
|---|---|---|
| 打开一份 PDF 会**重建 3 次**页面 DOM，每次都先清空再重新锚定 | 期间标记短暂消失（约 1–3s 窗口），用户看到的就是"没批注"；也浪费渲染 | P1：收敛为一次渲染 + 一次锚定 |
| 右栏卡片只增不减（连跑几轮后 22 张），列表变长后目标卡滚出视野 | 用户需要不断滚动；点击前需先滚动 | P1：分组/分页与"只看本轮" |
| `centerOn` 用 `smooth` 滚动 | 连续点击时滚动动画叠加，取样与体感都不稳 | P1：区分"手势滚动"与"定位滚动" |
| PDF 文字层的标点/空格分片被单独套 mark | 无视觉问题，但会放大 DOM 与计数噪声 | P2：合并同段相邻分片 |

---

## 6. 验收矩阵对照（§12）

| 编号 | 状态 |
|---|---|
| A01 PDF 真拖选跨行并写中文意见 → 保存/回读/重开可见绿虚线 | ✅ 通过（中文 + 英文样本，多次复跑） |
| A02 一条意见被 Agent 完成 → 原文变灰、新意见仍绿 | ⏸️ 未测（属 P2） |
| A03 原文整段替换 → 不在错位处画线 | ⏸️ 未测（精确偏移与指纹已就位，但未构造用例） |
| A04 重复句 / 跨页 / 特殊空白 / 旋转页 | ⏸️ 未测 |
| A05 点卡片 / 拖选文字 / 编辑 / 删除互不抢占 | ✅ 通过（10/20 次点击、拖选、编辑、引用） |
| A06 触控板连续缩放并滚动 | 🟡 CDP 合成通过 9/9；**真机触控板待人工验收** |
| A07 侧栏收起与调宽、无横向溢出 | ⏸️ 未测（P1） |

---

## 7. 下一步

1. 补 A03/A04 用例（重复句、跨页、旋转页、整段替换）——这是 B01 修复尚未被证伪的部分。
2. P1：把"打开 PDF 重建 3 次"收敛掉；右栏分组与滚动职责；`centerOn` 与手势滚动分离。
3. P1：三动作（批注/保留/删除线）与草稿/自动保存按 §3.5 复核，补 IME 与刷新恢复证据。

---

## 8. 复跑方式

```bash
# 1) 测试服务（独立 vault，不碰生产实例）
COEDITOR_PORT=4491 COEDITOR_STATE_DIR=/tmp/coeditor-p0/state node server.mjs /tmp/coeditor-p0/vault

# 2) 带 CDP 的独立 Chrome
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9333 --remote-allow-origins='*' \
  --user-data-dir=/tmp/coeditor-p0/chrome-profile --no-sandbox --disable-gpu

# 3) 套件
node tools/p0-b01.mjs                                   # 中文样本（默认）
COEDITOR_B01_DOC="研究设计-英文摘要.pdf" node tools/p0-b01.mjs
COEDITOR_B01_DOC="研究设计-技术附录.pdf" node tools/p0-b01-zoom.mjs
node tools/p0-b02.mjs
node tools/p0-b03.mjs
```

> 测试只杀自己启动的进程。生产实例（PID 39985 / 端口 4478 / vault `~/Desktop/简历/林木求职`）全程未被触碰，也未写入。
