# phase-report · P1：收敛日常路径 + 分类交底

日期：2026-09-11 · 依据：执行规格 §11 P1 / §3 / §12
状态：**P1 代码改动完成；测试结论按「实际通过 / 仅脚本通过 / 待人工验证」三类交底**
上一阶段见 `phase-report-P0.md`。本轮**未推送**，未触碰任何真实材料。

---

## 0. 先说结论（按可信度分级）

### 实际通过（真实鼠标路径 + 强断言，可直接采信）

| 项 | 证据 |
|---|---|
| 正文批注主回路（中文 3 页 PDF） | 17/17。含**三项一致性**：选中文本 = 落盘 `quote`（逐字）→ 重开标记文字 = `quote` → 标记几何 = 当初选区几何（误差 0，容差 3%） |
| 同上（英文单页 PDF） | 17/17 |
| 重复句：两处各自锚定 | 两条批注条数各 +1；`textOffset` 158 / 453，标记落在两个不同位置带 |
| 含全角空格 / 不换行空格的句子 | 能定位，标记贴合 |
| 跨页拖选 | 选区 68 字、能建批注、重开后**不画到拖选起点之上**（不跳文档开头/错段） |
| 反向用例：起点在正文之外 | **不产生批注**（不吞文档开头）——这条是修出来的真缺陷，见 §2 |
| 右栏点击 20 次 + 拖选 + 编辑 + 引用 | 9/9 |
| 批注栏/文件树调宽、收起、刷新记忆 | 17/17（含 180–320 / 240–400 夹紧、无横向溢出） |
| IME 组合输入 | 组合期间 0 次 POST、结束后 1 次、文本正确落入；**组合中切文件不提交半成品** |
| 保存失败路径 | 明确显示「未保存 · 请重试」、内容保留、不产生半成品；解除后重试成功落盘；刷新仍在 |
| 窄屏自适应 | <1100px 自动收起文件树；宽回来只自动展开「系统收的那次」 |

### 仅脚本通过（CDP 合成，不能当人工结论）

| 项 | 说明 |
|---|---|
| 触控板缩放（B03） | 合成 wheel 9/9（锚点漂移 0.1px、倍率随手势幅度）——**手感未经验证** |
| 响应式 1440/1100/900/760 | 15/18，剩 3 项见下 |

### 待人工验证（我做不了，交回给你）

1. **触控板手感**（约 30 秒）。
2. **旋转页真实拖选**：合成拖选在旋转文字层上取不到可靠落点（`caretRangeFromPoint` 取不到落在 span 内的点），已如实标为「前置条件不足」，**不是**产品失败，但也**没有**通过真实拖选取证。
3. **你自己的故障文档**：本轮所有结论都来自合成样本；你原来的那份 PDF 仍未被复测（未获授权，也没动它）。

---

## 1. 失败分类（按你要求分开报，不混在一起）

### A. 测试前置条件失败（改测试，不是产品问题）

| 现象 | 真实原因 | 处置 |
|---|---|---|
| 重复句第 2 条「选区工具条未出现」 | 上一步创建批注后的平滑滚动还没停，取到的坐标已过期；浏览器给出空选区（`len:0`，起点在正文内） | 取样前等滚动收敛 + 拖动前重取坐标 + 条件等待工具条 + 最多重试 3 次 |
| `openDoc` 超时 | 点击落在文件行边缘没生效 | 重定位重点一次；超时截图 + 现场 dump，错误信息标 `[前置条件失败]` |
| 测试台「拖动」没被当成拖选 | CDP 只给 `button` 不给 `buttons`，Chrome 不认这是按住拖动，选区只从上一个光标位置延伸 | 拖动事件补 `buttons: 1` |
| 旋转页取样点落在文字外 | 旋转后 span 的 AABB 中心不一定落在字形上 | 改用 `caretRangeFromPoint` 扫格点；仍取不到就如实标注 |

### B. 产品功能失败（已修）

**反向用例：从正文外起手拖进正文，把「文档开头到这里」整段吞成一条批注。**

- 起因是我自己上一版为修跨页而加的「选区与文档求交集」：起点在文档外时，我把起点补成了「文档开头」。
- 修法：**只裁尾巴、绝不裁头**。起点不在正文内 → 不当成正文选区（宁可不弹批注按钮）；终点在正文外（从正文里往外拖）才夹到文档末尾。
- 现在反向用例实测：**不产生批注**。

### C. 视觉 / 布局细节不符（已定位，未修）

**窄屏下 3px 横向溢出**（1100 / 900 / 760px，1440px 正常）。

- 已定位到具体元素，没有用 `overflow: hidden` 盖住：
  - `div#page` 宽 **820px**（纸张固定宽），`div#world` 868px；在 762px 视口下超出。
  - 内层 `div.pdf-page` 是 764px，**已经贴合视口**——所以是外层纸张容器没收窄，不是 PDF 渲染问题。
- 同轮修掉的另一处（真修掉，非掩盖）：`#cards-resizer` 原来是 `flex: 0 0 5px; margin: 0 -2px`，净占 1–2px 布局宽度，批注栏展开时把整行顶出窗口。已改为**零宽元素 + 溢出伪元素热区**，拖动命中面积不变、不再参与宽度计算。
- 待办：`updatePaperWidth` 在 PDF 模式下未按可用宽度收窄 `#page`。

---

## 2. 本轮代码改动

| 文件 | 改动 |
|---|---|
| `public/app.js` | ① `clampRangeToRoot` 只裁尾不裁头（反向用例）；② `selectCard` 不再在点右栏卡片时重建列表；③ 窄屏自动收起文件树（`applyNarrowLayout`，只自动展开系统收的那次）；④ 批注栏宽度夹紧改用 240–400、收起判定改用指针原始位置；⑤ 文件树宽度夹紧 180–320，默认 224 |
| `public/style.css` | 纸张/文件树默认宽走 `--rail-width`（224px）；`#cards-resizer` 改零宽 + 伪元素热区；批注栏收起时连分隔条一起隐藏 |
| `public/review.css` | `--rail-width` 统一为 224px（原来 240 覆盖了 style.css 的 280） |
| `tools/p0-harness.mjs` | 拖动补 `buttons`；`openDoc` 加重定位 + 超时留证；请求留痕 / 断网 / IME / 改视口 等能力 |
| `tools/p0-b01.mjs` | 新增三项一致性断言（文本、quote、几何） |
| `tools/p1-a04.mjs` | 新增反向用例；重复句/特殊空白改强断言（条数必须 +1）；旋转页前置条件分类 |
| `tools/p1-a07-a09.mjs`、`p1-responsive.mjs` | 新增（A07/A08/A09 与四档宽度） |
| `tools/make-a04-fixtures.py` | 生成 A04 合成样本（重复句 / 跨页 / 特殊空白 / 旋转页，A/B 两版） |

**纪律遵守**：没有给 `state` 注入合成数据当通过证据；没有放宽断言掩盖失败；没改真实材料；没推送。

---

## 3. 你只需要花 1 分钟验证这一段

1. 打开一个 PDF，拖选一句话（跨行更好）。
2. 点「批注」，写一句话。
3. **刷新页面**，确认那句话下面还有绿色虚线。
4. 用触控板慢慢捏合几次，看倍率是不是连续变化、手指下的位置有没有漂。

前 3 步对应「实际通过」里的主回路；第 4 步是唯一还需要你亲自感受的。

---

## 4. 环境与复跑

- 测试服务 `http://127.0.0.1:4491`（vault `/tmp/coeditor-p0/vault`，全合成样本）
- 测试 Chrome CDP `9333`，profile `/tmp/coeditor-p0/chrome-profile`
- 断言明细与截图：`/tmp/coeditor-p0/evidence/`
- 命令见 `phase-report-P0.md` §8；新增：

```bash
node tools/p1-a04.mjs          # 重复句 / 特殊空白 / 跨页 / 反向 / 替换
node tools/p1-a07-a09.mjs      # 调宽 / IME / 保存失败
node tools/p1-responsive.mjs   # 1440 / 1100 / 900 / 760
```

> 生产实例（端口 4478，vault `~/Desktop/简历/林木求职`）全程未被触碰、未被写入。
