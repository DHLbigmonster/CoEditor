# Changelog

## v1.4.1（2026-09-06）· 飞书式就地批注卡：点击正文高亮/批注就地浮出操作卡

按用户反馈（参考飞书文档的批注展现方式，保留选中三模式交互）：

- **点击正文锚点 → 就地浮出批注卡**：PDF 文字层、Markdown/HTML 正文里的任何批注标记，点击即在旁边浮出卡片——编号、类型徽标、内容、引用、操作按钮（取消保留/编辑/已处理/删除）就地可用，不必去右栏找。Esc 或点击外部关闭；浮卡对应批注被删除后自动消失；画布模式保留自己的卡片体系不受影响
- **反馈栏可拖拽调宽**（280–460px，记忆在本地）：此前折叠按钮虽工作，但宽度固定 300px 太窄
- **溢出排查结论**：隔离环境 4 缩放档位（100/115/175/300%）矩阵复测，正文区/纸面/work 层均无溢出、无工作台横滚——用户看到的"溢出"来自旧版 fixed 反馈栏（v1.4.0 已改布局同级双栏），需硬刷新（⌘⇧R）加载新前端
- **黑竖线二次定位**：像素级扫描确认现截图中已无缺陷竖线；历史截图的黑线来源即 #page 大扩散阴影（v1.3.5 已柔化）

电池 13/13。

## v1.4.0（2026-09-06）· UI 补充单：真双栏、标记语义、统一视图模型（未推送，待用户验收）

按 2026-09-06 UI 复审补充单（CoEditor-UI-Addendum-v1.3.5.md）执行 U06→U01→U02→U03→U04/U05，全部在隔离合成环境以真实鼠标验证（4461 端口），4400 未动。

- **U01 真双栏（P0）**：#cards 移出滚动容器，与 #viewport 构成布局同级的正文区+反馈栏。放大的纸面只在正文区内部滚动并被裁切，**反馈栏不可能再被覆盖**；工作台级横向滚动消失。几何断言：1440/200% 下 viewport.right == cards.left，反馈栏可点击
- **U02 标记语义（P0）**：保留 = 柔和半透明黄底（无边框/底线/阴影/padding）；stale/deprecated 状态规则跳过保留——此前同特异性覆盖出金色粗底边（截图 bug 根因）；废弃的保留不再画删除线
- **U03 统一视图模型（P0）**：待处理/保留/历史三组唯一定义，顶部计数、侧栏「当前」、列表筛选全部同源——修复「待处理 7/保留 0 却列出保留卡」的语义矛盾；保留+锚点丢失 = 「待定位」（要求仍有效）；历史默认折叠
- **U04 单一反馈入口（P1）**：顶部合并为「反馈」一个按钮（宽屏切右栏/窄屏切抽屉）；反馈栏改紧凑列表（去大卡边框阴影、正文与引用行数截断、低频操作进更多菜单）；保存后自动切到该条所在分组
- **U05 黑竖线（P1）**：DOM 命中探针定位——不是元素，是 #page 大扩散深阴影在浅底上的暗带；柔化为低对比阴影
- **基建**：eval 的 ws 依赖从已失效的 WorkBuddy 私有路径改为 devDependency；3 个 eval 硬编码端口清理；新增 accept-u05.mjs 综合验收脚本（几何/状态/鼠标流程断言）

电池 13/13。**未解决/下一轮**：PDF 文字层跨行保留的逐行矩形合并（官方 TextLayer 的 span 结构下 mark 高亮已按行，无需药丸化）、HTML 外链的逐资源白名单、反馈栏宽度可拖拽调节、更多菜单的键盘可达性细节。

## v1.3.5（2026-09-06）· 阅读版式定稿：文档 = 固定版心纸面，HTML = 自适应网页

按用户反馈定稿阅读版式模型（此前"文字自适应填满可用宽"是错误方向）：

- **md/txt/docx/json/csv = 固定版心纸面**（理想 820px）：像 Word/PDF 的页面——不随窗口/侧栏伸缩，100% 时自动适配可用宽（小窗口不横滚），放大是**整张纸变大**并产生内部滚动，右栏反馈栏永远钉在原位
- **HTML 例外保持自适应**：网页跟随容器宽度响应式变化（拉宽窗口页面跟着变宽），高度按内容——这是网页的天性，不按内容撑宽（上一轮按内容宽度反而造出超宽页面）
- **文本文档阅读模式隐藏引导线**：反馈栏改固定侧栏后线的坐标体系已失效（此前画到错误位置），且 12000px 的 SVG 画布会把 overflow:auto 的正文区撑出无限假横滚；悬停联动保留，图片/PDF/画布的区域线不受影响
- 「适合宽度」= 缩放因子回到 1（纸面适配可用宽），点缩放百分比 = 回 100%，与浏览器/PDF 查看器习惯一致

电池 13/13。

## v1.3.4（2026-09-06）· F12 前置安全：可选访问令牌 + HTML 预览外链策略

WebUI 部署路线的安全前置（本地单人使用行为完全不变）：

- **可选访问令牌**：启动时设 `COEDITOR_TOKEN=你的口令`，所有请求需携带令牌（登录表单落到 HttpOnly cookie / `?token=` / `Authorization: Bearer` 三种方式），未带则 API 返回 401、页面返回极简登录页（错误令牌有提示）。不设置时行为与之前完全一致。CLI 可用 `COEDITOR_TOKEN=x node cli.mjs ...` 自动携带
- **HTML 预览外链策略**：预览 iframe 注入 CSP——不执行脚本（原有 sandbox 之外双保险）、不跑 fetch、不嵌套框架；本地相对资源与远程图片/样式照常渲染。策略是显式的：本地批注层不做匿名代理
- **F07 收尾**：扫描件（无文字层）PDF 明确提示「无法选择文字，建议区域批注」，不承诺不存在的 OCR；跨屏拖动时 DPR 变化自动触发 PDF 重渲染（不拉伸旧位图）
- **保存入口异常捕获**：批注保存失败落在用户眼前（composer 保持打开 + 明确 toast），不再变成全局「操作未完成」

电池 13/13。

## v1.3.3（2026-09-06）· F07：PDF 官方文字层、渲染失败可重试、懒绘制链条修复

按审查报告 F07 推进（pdf.js 6.3.289 的 `TextLayer` API 可用）：

- **官方 TextLayer 替换手工近似 span**：文字层直接用 pdf.js 官方 `TextLayer` 渲染——与页面 viewport 完全一致的排版度量（字体、字距、旋转），选择行为与官方查看器一致。此前手工按 `left/top/fontSize` 拼 span 并强制 Helvetica，是无法正确处理 PDF 度量的根子。锚定系统（wrapRange 注入 mark）与官方层兼容，回归全绿
- **渲染失败不再静默**：单页 `page.render` 抛错此前被 `.catch(()=>{})` 吞掉、painted 过早置真——空白页冒充加载成功。现在失败页显示明确错误与「重试」按钮，成功才记 painted
- **修复一个会让全部页面停止渲染的链条污染**：首屏 paint 项缺 wrapper 引用，catch 里的二次异常把 paintChain 打成 rejected，后续所有页面的按需绘制被永久跳过（12 页文档只画 1 页）
- **TextLayer 并行渲染**：占位结构按页序即时出现，官方文字层 12 页并行构建——首屏不再等待长文档的串行 DOM 构建（报告 A15 的第一半）
- eval-recent 的列表计数改为轮询等待（异步渲染竞态曾误报 0 条）

电池 13/13。**未做**：扫描件无文字层的明确提示（区域批注可用但要有说明）、DPR 变化动态重绘（跨屏拖动场景）。

## v1.3.2（2026-09-06）· W2/W3 收尾：引导线实时端点、历史卡片可读、缩放跟随尺寸变化

按 2026-09-06 审查报告继续推进（F06 / F05 / F03 收尾）：

- **F06 · 引导线实时端点**：拖动批注卡时同步内存坐标——此前 annotation.x/y 到松手才更新，画线却读旧值，导致拖动全程引导线钉在旧位置（已按报告指出修复）
- **F05 · 历史卡片可读**：`focus-active`（默认开启）曾把所有非当前批注卡压到 opacity 0.14——空间模式下几乎不可见。现在弱化为 0.62 并在悬停/选中恢复全不透明；「历史要有层次，但不能隐形」
- **F03 收尾 · 缩放跟随尺寸变化**：「适合宽度」之后拖动侧栏分隔条或改变窗口大小，正文自动按新可用宽度重新适配（保留缩放语义：手动 ± 缩放后不再自动跟随）
- **MCP initialize 版本号修正**：此前硬编码 `0.9.2`，无法用于判断服务版本；现从 package.json 读取

电池 13/13。**仍未做（下一轮）**：F07 官方 PDF 文字层与渲染错误状态、F05 的状态四维完整拆分（intent/anchorStatus/displayRound）、F10 版本对照主视图、图片工作区手势隔离、F12 剩余（vault 身份强制、HTML 外链策略）。

## v1.3.1（2026-09-06）· 阅读缩放修复：放大只作用于文档，右栏永远不被顶走

用户实测反馈（65%/108% 截图）：放大后正文被右栏遮挡、缩放逻辑「自创」。本轮对齐浏览器/PDF 阅读器的通用语义。

- **阅读缩放只作用于文档内容**：右侧反馈栏改为固定侧栏（窗口右缘，像 PDF 阅读器），放大时正文在正文区内部滚动，不再推挤/遮挡右栏，工作台级横向滚动彻底消失
- **PDF 真缩放**：此前 PDF「放大」实际被 flex 压回容器宽（画布重绘但布局不变，是假放大）。现在缩放因子真正传入 pdf-layer：放大 = 页面实际变大 + 内部滚动，「适合宽度」= 单页适配容器（缩放后阅读位置保持）
- **适合宽度真实计算**：按正文可用宽度（扣除反馈栏/内边距）收敛到不与任何栏重叠，不再自创「重置为 100%」；点缩放百分比 = 回到 100%（浏览器习惯）；缩放范围放开到 50%–300%
- **右栏可折叠**：顶栏「反馈栏」按钮一键隐藏/显示（记忆在本地），隐藏后批注走「批注」抽屉；默认宽度 318→292
- **HTML 按内容原始宽度阅读**：固定设计宽的页面（简历）不再被 960 容器压窄
- **Word 诚实化**：docx 是只读预览（架构如此，修改走「批注 → 交给 Agent 改稿」），编辑入口改为明确的禁用态 + 说明，不再点了没反应
- 修复两处连锁：fixed 反馈栏层级盖住顶栏下拉菜单（stacking context）；HTML 框架宽度自适应

电池 13/13。桌面 APP 路线按用户决定移除（产品形态 = 浏览器 WebUI）。

## v1.3.0（2026-09-06）· 可信性修复（W1 优先级）：不再假保留、假回应、假历史

按 2026-09-06 独立审查报告执行 W0/W1，四个探针反例全部先转为正式回归（`tools/test-contracts.mjs`，修前红、修后绿），再修实现。**诚实声明：F03（缩放体系分离）、F05（状态四维拆分）、F06（连线实时端点）、F07（官方 PDF 文字层）、F10（版本对照主视图）、图片工作区隔离本轮未做**，见文末。

**F01 · 保留校验与模糊定位分离（P0，反例已复现）**
- 新增 `verifyRetainedText`：完整原文**精确存在一次**才叫 intact；重复出现且无法消歧 = ambiguous 待确认；只剩 prefix 残留或前 12 字相同 = missing；HTML 等标记文本 = unverified（明确承认没校验过），四种情况都不再谎报「保留成功」
- `offsetsOf` 降级为纯定位建议（仅锚点用途）；保留继承（carry）与对照汇总全部改走新校验

**F04 · 回应与退回绑定「这一次改稿」（P0，两个反例已复现）**
- 「回应了 N 条」改按最近一次 Agent 回应记录（`roundHistory` 的 resolved 列表）取数——批次推进后不再假报 0
- 退回只重开**本次回应记录里**的批注：无关历史保持已接受；没有回应记录时退回只改版本状态；批次计数单调前进，不再随退回回滚

**F09 · 历史对照改读快照（P0）**
- 登记时哈希与快照来自**同一次读取的字节**（消除登记中途被改的窗口）；幂等身份纳入 sourceHash——原稿变了另立新条目
- `/api/versions/diff` 有快照一律读快照（`basis: "snapshot"`），活动文件后来怎么改都偷换不了历史对照；前端排版并排同样读快照；新旧差异照旧阻止误验收

**F02 · 文档会话身份（P0，核心部分）**
- `docEpoch`：每次打开文档递增，openDoc/批注加载/保存/版本预取的迟到响应全部作废，A 文档的响应不再写进 B 文档
- 保存捕获提交文本：请求在飞行中又打字 → 盘上是提交版、界面保持 dirty 并提示再按 ⌘S，不再把没写盘的内容当已保存；提交成功后基线 mtime 前移，连续保存不误报冲突
- 保存徽标改为枚举状态（clean/dirty/saving/error/conflict），不再靠一段文字猜

**F12 · 写入边界（P0，核心部分）**
- 启动与切 vault 时 ROOT 归一化到真实路径；新增 `safeResolveReal`（symlink 越界拒绝），`/api/write`、`/api/create-file`、`/api/save-brief` 全部改走真实路径校验
- `/api/write` 的 `baseMtime` 成为必要前置：静默省略不再能绕过冲突保护

**F08 · 批注操作不再重建整份文档（P1，第一步）**
- 批注增删改后的刷新改为只更新批注层（`loadAnnotations({ rerender: false })`），PDF/iframe 不再因改一条批注重载；外部修改与文档切换仍走全量

**F11 · 回归修复**
- PDF/图片分支不再重复插入反馈头部；大纲点击导航统一 `scrollIntoView`（跨 iframe 滚祖先链）

**F10 · 空白语义 diff（P1）**
- textDiff 不再 trim 行、不再丢空行：嵌套列表改平级、缩进变化如实显示为增删（探针反例转正）

**测试与门禁**：契约 11/11（新）+ 后端 9/9（旧）+ 电池 13/13（版本套件扩到 27 断言，新增快照对照稳定性、退回不重开无关历史、批次不回滚）；测试基础设施随 ROOT 真实路径归一化同步修正。

**本轮未做（下一轮，按报告 W2–W4）**：阅读缩放与画布 transform 分离（F03）、官方 PDF 文字层与渲染生命周期（F07）、批注状态四维拆分（F05）、引导线实时端点（F06）、版本对照进入主工作区（F10 布局部分）、图片工作区与对象/相机手势分离（W4）、vault 会话身份强制与 HTML 外链策略（F12 剩余）、DocumentSession 完整抽象（F02 剩余：取消请求 AbortController、内容修订号）。

## v1.2.0（2026-09-05）· 愿景图收尾：大纲面板、排版并排、保存徽标

把产品愿景图与当前实现最后三项差距补齐（按价值排序）：

- **右栏「大纲」面板**：右栏顶部新增「大纲 | 反馈」两个视图。大纲自动提取正文的 h1-h3（Markdown 在渲染 DOM 里取，HTML 从预览 iframe 里取），点一下平滑滚动到对应位置并高亮。长文档从此有了结构导航
- **版本对照默认「排版并排」**：修改前/修改后不再只是段落文字对齐，而是**渲染后的文档**左右并排（Markdown 走与阅读模式同一套 renderMarkdown，HTML 用原始保真源码），各自装进禁脚本的 iframe（`sandbox=""`，纯展示不执行内容）。工具条可切「文本」回到逐段对齐，可切「逐段」看变化列表
- **顶栏常驻保存徽标**：「✓ 已保存到本地」一直在顶栏可见（保存中变黄点、失败变红「未保存 · 请重试」），与侧栏底部的同步状态联动——本地优先的产品承诺第一次有了常驻表达
- 电池版本套件扩到 26 断言（保存徽标 / 大纲提取+点击导航+高亮 / 排版 iframe 双栏渲染全入门禁）；13/13 通过

已知边界：大纲高亮是「点击后定位」，暂不做滚动跟随（跨 iframe 的 IntersectionObserver 需要单独方案）；排版并排在侧栏宽度下每栏较窄，需要大屏并排时可再加分屏视图。

## v1.1.0（2026-09-05）· 版本对照升级为「并排双栏 + 回应汇总」；界面全面转中性简洁风

按产品愿景图（阅读/编辑/版本三态）补齐差距，视觉从暖橘「小清新」整体换成 ChatGPT 官网式的简洁干练（白底、黑字、黑主按钮、彩色只保留功能语义）。

- **并排双栏对照**（版本 Tab 默认视图）：相邻的「删旧 × 增新」按顺序配对成左右两列，一眼看清「这句换成了那句」；连续未变化段落折叠、点开展开；可切回「逐段」视图
- **回应汇总**（愿景图头部那句）：对照顶部显示「回应了 N 条反馈 · M 处保留内容未改动 / ⚠ K 处保留内容在新稿中未找到，待确认」——保留核对在服务端逐条做，不做前端估算
- **退回 = 否决 Agent 的「已处理」声明**：退回版本时该文档全部已处理批注重回待处理（保留标记不受影响），批次号同步回退到登记前一轮；此前退回只是换个标签，已处理的反馈永远留在归档里
- **视觉改版**：主题色从陶土橘 (#ee6b50) 换成近黑 (#0d0d0d)，米色系背景换成中性灰阶，成功色换 ChatGPT 绿 (#10a37f)；正文黄底「保留」标记、错误红是功能色，保留不动
- 电池版本套件扩到 23 断言（并排渲染、汇总行、退回重开+轮次回退全部门禁）；13/13 通过

## v1.0.1（2026-09-05）· 版本对照可信化：不会验错、不会丢保留、不会假装保存成功

> v1.0.0 宣布正式版时，只读审查发现版本闭环仍有一批「能假绿」的缺陷（验收错对象、漂移后照常验收、继承静默失败、锁可被抢、队列可饿死）。本版按「先复现测试、再修实现、不用放宽断言换全绿」逐项修复，修完后电池 13/13 才是可信的全绿。

针对只读审查（v0.9.9 / `1f069b2`）列出的 P0/P1 问题逐项修复，原则：先补复现测试，再修实现，不用放宽断言换全绿。

**P0-1 请求治理与锁边界**
- 修「页面能开、API 不响应」：全局请求队列排队等待上限 15s（超时 503 且**必须放行后队**）、请求 body 读取上限 20s（挂起的客户端不再堵死所有人）。已验证：占住队列的坏请求期间，`/api/tree` 15s 内得到 503 而非永久挂起
- async handler 统一 catch：任何异常不再变成 unhandledRejection 打死进程（Node 22 默认 crash）；锁释放失败不外抛（最迟随心跳过期被回收）
- `/api/versions` 的登记/验收/继承纳入 store 锁：HTTP 与 MCP 不再可能同时改 sidecar 互踩丢记录

**P0-2 版本身份与快照**
- 每个版本有不可变 `id`；验收按 id 定位，**不再用列表序号**（新增版本后序号移位会验收错对象——已复现并加入测试）
- 登记时保存源稿/新稿 sha256 与原样快照（`.marginalia/version-snapshots/`）；验收前服务端重读文件核对哈希，内容被调包的验收返回 409 `version-content-changed`
- 路径先 realpath 规范化再比较：`base.md` 与 `./base.md` 现在会被正确识别为同一文件并拒绝
- 同内容重复登记 = 幂等：**不再把已验收状态重置回 pending**；内容变化后重新登记 = 新条目，历史保留

**P0-3 服务端保留继承**
- 继承从「前端逐条 POST、仅当新版无批注、失败静默」改为服务端事务（`POST /api/versions` `action=carry`）：按来源批注 id 幂等补齐，重复执行不堆叠、中途失败可再补
- 逐条在新稿文本里核对原文：找不到的保留标记明确标记 `anchorStatus=missing` + 「保留内容缺失，待确认」，绝不静默成功

**P1 对照诚实化 + 窄屏**
- diff 改为保留顺序的 LCS：段落重排如实显示「删旧 + 增新」（旧实现显示 0 变化——已复现并加入测试）；变化全量展示不再截断，未变化段落折叠可展开；「保留 X 段」改称「未变化」，与人设置的「保留要求」区分
- 文件与登记不一致时对照顶部黄条明示；二进制成品（PDF/DOCX）不再硬按 UTF-8 对照，诚实提示不可对照
- 窄屏抽屉接入同一版本面板组件（此前 <1180px 时版本功能只剩空入口）；「版本对照」Tab 计数改用真实版本数（旧实现错用批注数组）

**P1-3 测试门禁重做**（eval-versions 20 项断言，旧的 `openedNewVersion=true` 硬编码断言已删除）
- 验收错位：UI 停在旧条目 → 背后登记新版本 → 点验收 → 被验收的必须是用户看到的那条，干扰版本必须保持 pending
- 漂移阻止、幂等登记、同文件多路径拒绝、重排检出、继承幂等、继承缺失报警、真实 URL 跳转 + `carriedRetained>0` 全部入门禁
- 电池 13/13（发布门禁通过）；套件改为只动自己的子目录，不再清共享 vault（旧版会删掉其他套件的文档）

## v0.9.4（2026-09-04）· 批注卡与正文互链

- 悬停任一侧即点亮其余侧：抽屉条目、画布卡、正文锚点、区域框四者联动。之前悬停卡片只点亮牵引线、正文文字没反应，悬停正文则完全无响应
- 视觉用**描边**而非改底色，避免与保留（黄）、删除线、已处理（绿）的语义色混淆
- 正文锚点每次渲染都重建，正文侧用事件委托（iframe 文档每次导航重新绑定），不是逐元素绑定
- 电池第 11 套件：卡片→正文、正文→卡片双向点亮，以及移开后复位

## v0.9.3（2026-09-04）· 修改指令可落盘

- 「交给 Agent」除复制外，可把修改指令**存成 .md 放进当前目录**（沿用 `<文档名>-修改指令.md` 命名，与「改写提案/润色提案」一致）。Agent 有目录读权限时自己读文件即可，不必来回粘贴
- 同名自动加 `-2/-3` 后缀，写盘用 `wx` 保证**绝不覆盖**已有文件；文件名沿用既有越界消毒
- 指令头补生成时间，多份指令可区分先后
- 品牌与门面：logo 13 个文件入仓（`docs/logo/`）、README 顶部加主标、网页补 favicon（SVG + PNG 兜底）
- 电池第 10 套件：指令落盘 / 不覆盖 / 文件树可见 / 指令含批注号与目标文件

## v0.9.9（2026-09-05）· 保留标记跨版本继承

- 「继续批注新版本 →」自动把上一版生效中的「保留」标记带到新版本文件（新版本无批注时执行一次，防重复堆叠；锚定由新文本模糊重定位完成）
- 原型承诺「保留的内容持续有效」闭环：v0 标记保留 → Agent 改出 v1 → 验收 → 继续批注 v1 → 保留标记继承 → 循环
- 「交给 Agent」指令补版本工作流选项：大改写可另存新版本文件（不覆盖原件），人验收后采用
- 电池 13/13（含版本对照闭环套件）

## v0.9.6（2026-09-05）· 版本对照闭环 + 电池健壮性

- **版本对照闭环**（原型「这一轮，改了哪里？」落地）：Agent 经 MCP `register_version` 登记新版本（另存旁文件、绝不覆盖原件）→ 反馈面板「版本对照」Tab 列出待验收版本 → 段落级对照（新增/删除/保留 + 摘要）→ 「验收 / 退回」→「继续批注新版本 →」
- lib/version.mjs：版本登记（同轮同文件更新不堆叠）/ 状态机（pending→accepted|rejected）/ 段落级 textDiff
- server：`/api/versions`（GET/POST register|decide）+ `/api/versions/diff`
- 电池健壮性：端口残留清理 + root 一致性校验 + 套件间 tab 清理 + 失败自动重试 + 就绪等待（修复「混合代码首跑全红」的环境根因）
- PDF 渲染器等待 6s→20s（高负载下不再误报「渲染器未能加载」）

## v0.9.5（2026-09-05）· 并发事务正确性（接手 Codex 第二次限额中断）

- **修复真 bug：sidecar 跨进程锁竞态**——原实现用「目录」当锁，释放是 unlink + rmdir 两步，并发下另一方可在两步之间抢到锁、随后被前一方的 rmdir 抹掉，出现多个持有者（表现为 EEXIST 外泄 / 批注丢失）。改为**独占创建单个锁文件**（获取与释放都是原子操作），带持有者存活检测、陈旧锁回收（30s TTL）与 15s 超时保护
- 新增 `withStoreTransaction` 统一入口（HTTP 的路由层与 MCP 的 dispatch 层都已持锁，整个 RMW 在锁内）
- 测试适配（产品行为是对的）：阅读模式批注分「待处理 / 保留 / 历史」三组后，断言需先切到对应 Tab；高负载下脚本等待时间延长；recent 判据与测试自身口径对齐
- 验证：`node --test tools/test-review.mjs` 9/9；`node tools/run-battery.mjs` **12/12**
- 含 Codex 的 v0.9.4（批注卡 ↔ 正文悬停互链）与 v0.9.5（多 vault 最近打开记录）

## v0.9.2（2026-09-04）· 批注工作流闭环

- 侧栏「＋」新建文档（Markdown / TXT / HTML，模板初始化，重名不覆盖）
- MCP 新工具 `resolve_annotations`：Agent 处理完批注后自动标记「已处理」，网页自动灰显归档——**人不用再点任何按钮**
- 已处理批注在抽屉中灰显半透明；PDF 工作流占位页改为真实可执行的「截图→区域批注→交给 Agent→并排验收」向导
- HTML 直改的源码定位增强（多重候选 needles + 最近邻匹配）；顶栏减负（归档/交给 Agent 移入 ••• 菜单）
- 电池第 9 套件：新建文档 / 定位源码 / MCP resolve 闭环 / 顶栏减负（11 断言）

## v0.9.1（2026-09-04）· 一致性巡检

- MCP `brief` 补 `kindLabel` 中文类型（保留 / 删除线 / 区域）与版本号，供 Agent 直接用人话引用

## v0.9.0（2026-09-04）· 直觉交互收口

- 「高亮」改为「保留」：明确表示原文很好、后续修改不得删除或改写；保留标记只显示黄底，不绘制牵引线，可随时取消
- PDF 文本层标记文字保持透明，修复 canvas 正文上叠出第二层文字的重影
- 面向人的编号统一为 `批次-序号`（`0-1、0-2…`）；内部 `A-xxxx` 只用于兼容旧数据
- 批注历史以绿点/淡化表达当前与历史；普通批注支持编辑、删除，显式删除前自动保留 `.prev`
- 左侧改为 VS Code 式折叠文件树；macOS「打开文件夹」调用原生系统选择器，网页目录浏览仅作兜底
- 图片区域框选改为单 pointer 捕获，防止卡顿时生成多个框；图片加载完成后重算区域与连接线，修复漂移
- 图卡 pointer 拖动与画布平移隔离；图卡只移动自身并持久化坐标；工具条固定在画布底部中央
- 移除便签、白板入口与快捷键；旧数据保留但不再渲染或进入 Agent 约束
- 发布门禁扩充为 8 套，新增树折叠、显示编号、批注编辑删除、区域防重复、图卡独立拖动等真实交互验证

## v0.8.1（2026-09-04）· Agent 感知与交接

- UI 状态、约束读取与开发交接文档补齐

## v0.8.0（2026-09-04）· 三模式架构稳定版

测试电池 `tools/run-battery.mjs` 一键 7/7 全绿（真实 CDP 鼠标事件，隔离 vault + 隔离端口），作为发布门禁。

- 阅读 / 编辑 / 画布三模式（默认阅读：宽度自适应、纵向自然滚动、批注卡右侧同屏）
- 暖白纸感界面；侧栏 220–480px 拖拽记忆；顶栏精简（次要视图项收进 ••• 菜单）
- CodeMirror 源码编辑器（语法高亮 / 行号 / 撤销 / ⌘F 搜索 / 当前行）；HTML 编辑带实时预览分屏
- HTML 隔离 iframe 真实网页呈现 + 双击段落直接改文字（双树安全回写：URL / 脚本 / 结构原样保留）
- PDF HiDPI 渲染 + 长文档按需绘制（IntersectionObserver）；区域框选批注；列布局 1/2/3
- 批次系统（人话语言：完成本轮批注 / 交给 Agent；外部文件变化不自动推进）；批注筛选三态
- 数据安全四层：防破坏守卫 / 读失败拒绝写 / mtime 乐观锁 / 写前自动备份（.prev）

## v0.7.x（2026-09-04）· 三模式重构系列

v0.7.0 三模式重构（接续 Codex WIP 补完）· v0.7.1 批注筛选三态 · v0.7.2 CodeMirror · v0.7.3 HTML 双击直改 · v0.7.4 搜索+实时预览 · v0.7.5 PDF 按需渲染 · v0.7.6 边缘打磨

## v0.6.x（2026-09-04）· 协作闭环强化

可视化文件夹选择器 · PDF 选区批注解禁 · 箭头层拦截修复 · vault 切换防批次虚推进 · PDF 区域框选批注 · 批注卡降噪 · sidecar 写前备份

## v0.5.0（2026-09-04）· 编辑与批次

Markdown 编辑双态 · 选区浮条（批注/高亮/删除线）· 批次系统 · 白板 · 切换 vault · sidecar 空壳覆盖事故根治

## v0.4.0（2026-09-04）· Cowart 交互层

手绘标注箭头（建完即写标签）· 便签 · 贴图 · Agent 双向画布（get_canvas_state / insert_canvas_image / insert_html_draft）· 防破坏保存

## v0.3.0（2026-09-04）· 首个公开版

画布式批注层 · PDF/图片批注 · 内嵌图片批注 · 牵引线 · 矛盾检测 · MCP 七工具 · CLI · Obsidian Canvas 导出

## v0.1–v0.2 · 内部原型

批注层核心（锚定/状态机/权重衰减/模糊重定位）· PDF.js 集成
